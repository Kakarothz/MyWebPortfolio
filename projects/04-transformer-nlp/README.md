# 04 — Transformer NLP

**Status: Implemented**

Minimal pretrained Transformer sentiment-inference demo using Hugging Face Transformers and DistilBERT.

## Run
```bash
pip install -r requirements.txt
python app.py
```

The first run downloads the pretrained model. This project demonstrates inference; it does not claim that the pretrained model was trained by this repository.
